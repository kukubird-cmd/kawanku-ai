// MindBuddy optional browser-side Gemini key.
// Preferred: set GEMINI_API_KEY on the Flask backend so the key is not exposed in the browser.
// Use this only as a local fallback if you intentionally want the frontend to call Gemini directly.
window.GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY_HERE';
